# 数据源排查报告（2026-09-03）

针对「5 路不可用」的逐路定位，以及替代接法的实测结果。**结论先说：雪球 3 路和财联社在免费 + 自动化条件下连不上，是站点侧风控，不是代码问题；但消息面已找到等量替代，整体有效产出从 6/12 提升到 8/14。**

---

## 一、逐路诊断

### 雪球 3 路（关注榜 / 讨论榜 / 交易榜）—— Geetest 行为验证码墙

| 排查步骤 | 结果 |
| --- | --- |
| `xueqiu.com` 首页 | HTTP 200，110 KB，**可访问** |
| `xueqiu.com/S/SH600000` 个股页 | HTTP 200，可访问 |
| `xueqiu.com/hq` 热榜页 | HTTP 200，**成功下发 `xq_a_token`**（764 字节完整 cookie） |
| `stock.xueqiu.com/v5/stock/hot_stock/list.json` | HTTP 200，返回 **7 KB HTML**，含 `captcha` + `geetest` 特征 |

**关键结论**：页面域名能访问、还能拿到 token，说明**不是 IP 黑名单**（此前记录的"IP 风控"诊断不准确，已修正）。拦截发生在 `stock.xueqiu.com` 这个 API 域名上，是**极验（Geetest）行为验证码墙**。

实测对比过一个易错点：手动只带 `xq_a_token` 一个 cookie 会丢掉 `acw_tc` 等 WAF cookie，反而更容易被拦；带完整 cookie 串也一样返回验证码页 —— 证明**与 token 无关**。

**可行解**：住宅出口 IP / 人工过一次滑块后复用 cookie / 付费接口。纯代码无解。

### 财联社电报 —— SPA 空壳 + 带签名 XHR

试了 5 种接法，全部失败：

| 接法 | 结果 |
| --- | --- |
| PC 电报页 `www.cls.cn/telegraph` | 200，有 `__NEXT_DATA__`，但**正文 0 条**（SPA 空壳） |
| 移动电报页 `m.cls.cn/telegraph` | 200，75 KB，同样 **0 条代码** |
| `nodeapi/telegraphList`（免签） | 返回 HTML，非 JSON |
| `v3/depth/home/assembled/1000` | JSON 仅 31 字节，空 |
| `api/sw` | JSON 仅 49 字节，空 |

另外 akshare 的 `stock_info_global_cls` 会**无限期挂起**（实测 >3 分钟不返回），已从采集流程中排除。

**关键结论**：电报正文走带 `sign` 参数的 XHR，需要还原签名算法。**可行解**：逆向签名 / 付费接口。

### 同花顺要闻 —— 偶发超时，已修复

上次采集 70s 超时，本次实测 **84ms 返回 20 条**。属于偶发性网络抖动，不是稳定故障。已在采集器里加了 1 次重试，下次再遇到瞬时卡顿会自动重来。

---

## 二、替代方案与实测收益

既然消息面缺了财联社，就找能顶上的：

| 源 | 条数 | 命中 A 股 | 状态 |
| --- | --- | --- | --- |
| **东方财富·全球财经** | 200 | **38 只** | ✅ 已接入，消息面新主力 |
| 同花顺·要闻 | 20 | 1 只 | ✅ 已接入（加重试） |
| 新浪·全球财经 | 20 | 0 只 | ⚠️ 已接入，本批无命中 |

**东财全球财经以 38 只的覆盖，完全顶替了原本只有 5 只命中的同花顺**，消息面不但没削弱，反而增强了。

### 采集结果对比

| 指标 | 修复前 | 修复后 |
| --- | --- | --- |
| 数据源总路数 | 12 | **14** |
| 有效产出 | 6 | **8** |
| 消息面覆盖 | 5 只（同花顺） | **38 只（东财全球财经）** |
| 覆盖个股 | 5203 | 5201 |

---

## 三、诚实说明：做不到的事

1. **雪球 3 路、财联社这 4 路，我不打算假装能连上**。它们是站点侧的行为验证和签名校验，硬爬要么违法要么不稳定。页面里会明确标注失败原因和解决路径，不静默吞掉。
2. **新浪全球财经本批命中 0 只**，只给 20 条快讯，样本量太小，别当信号用。保留它是为了增加时间覆盖，不是因为它现在有用。
3. **金十数据接口通但本批无 A 股代码提及** —— 属于正常波动（快讯内容随行情变），不是故障。
4. **消息面整体样本仍然偏小**：东财全球财经的 38 只是单次 200 条新闻的快照，不是全量。用它做"排除冷门股"比"发现黑马"更可靠。

---

## 四、本轮同时修掉的其他问题

排查过程中发现并修复了 3 个与数据源无关、但影响更大的问题：

| 问题 | 根因 | 修复 |
| --- | --- | --- |
| **四个模块全部空白** | 数据脚本插在 `</body>` 前，而 4 个模块的 IIFE 是解析即执行，跑在数据定义之前 | 改为插在 `<body>` 开头；生成源固定用 `index_src.html`，保证幂等 |
| **中际旭创 822 元被评"值得关注"** | 分析引擎漏了"股价 ≤ 120"这条可执行铁律 | 新增**第零道闸门**：股价上限 + 单只 6.5% 预算能买几手，通不过直接判"不可执行"并压到 −3 分 |
| **主力资金流 / 板块资金流为空** | 上次采集被中断 | 重采完成：个股资金流 5208 只、行业 15 + 概念 15 个板块 |

第零道闸门实测拦下 6 只高价股：中际旭创 822.4 元、北方华创 667 元、罗博特科 582.7 元、协创数据 262.05 元、胜宏科技 225.18 元、生益科技 144.98 元 —— 这些票因子分可能很高，但 20 万本金买不出合规仓位。

---

## 五、相关文件

| 文件 | 作用 |
| --- | --- |
| `web/index_src.html` | 干净模板（不含数据），所有改动的唯一入口 |
| `web/build_static.py` | 把 4 份 JSON 内嵌进模板，生成 `index.html` |
| `web/modules/sentiment/fetch_sources.py` | 14 路采集器（新增 `em_news` / `sina_news`，同花顺加重试） |
| `web/modules/sentiment/dump_status.py` | 子进程隔离编排，单源超时可配 |
| `web/modules/sentiment/probe_*.py` | 本次排查用的探测脚本，保留备查 |
| `web/modules/analyze/analyze_stock.py` | 个股分析引擎（新增第零道闸门） |
| `web/modules/mainfund/fetch_fund.py` | 主力/机构/龙虎榜采集 |
