# A 股量化投资系统（20 万本金实盘）

每日开盘前产出明确到「买哪只、几手、多少钱」的建仓清单。

---

## 快速开始

```bash
# 1) 启动网页（在线模式，5 个模块全部可交互）
cd /workspace/web && python3 server.py 8899
# → http://127.0.0.1:8899

# 2) 手动生成当日信号（会自动落盘 + 刷新静态离线版）
bash /workspace/web/run_daily_signal.sh
```

离线版直接双击 `/workspace/web/index_static.html` 即可（数据已内嵌）。

---

## 目录结构

| 路径 | 说明 |
|---|---|
| `web/index_src.html` | **页面唯一改动入口**（干净模板）。所有注入脚本都基于它，保证幂等 |
| `web/index.html` / `index_static.html` | 由 `build_static.py` 从模板生成的产物，**不要手改** |
| `web/build_static.py` | 把 API 数据内嵌进 HTML，生成离线版 |
| `web/server.py` | Flask 后端，6 个 API |
| `web/run_daily_signal.sh` | 每交易日开盘前的信号生成入口（cron 挂这个） |
| `web/modules/signal/daily_signal.py` | **信号生成器**（六因子 + 三闸门 + 整手下单测算） |
| `web/modules/paper/engine.py` | 模拟盘引擎（真实摩擦成本） |
| `web/modules/paper/quote.py` | 实时行情层（腾讯主 / 新浪备） |
| `web/modules/sentiment/` | 14 路情绪源采集 |
| `web/modules/analyze/` | 个股分析（含第零道闸门：可执行性） |
| `web/api/*.json` | 各模块数据快照 |
| `web/api/positions.json` | **个股腿持仓与成本价**（止损扫描依赖，需人工补录） |
| `web/api/daily_signal.json` | 当日信号（前端与模拟盘的唯一数据源） |
| `daily/{日期}_信号.md` | 每日信号归档 |
| `logs/signal_{日期}.log` | 每次运行的完整日志 |
| `data/kline_cache/` | K 线缓存（带 `_manifest.json` 记录 bar_date） |

---

## 每日信号机制

### 调度

| 项 | 值 |
|---|---|
| 时间 | 工作日 **08:30**（Asia/Shanghai） |
| 通道 | 项目定时任务「每日 8:30 生成 A 股交易信号」（id 7025967） |
| 脚本 | `web/run_daily_signal.sh`（非交易日自动跳过，失败重试一次） |
| 耗时 | 首次约 95 秒（641 只 K 线），命中缓存约 7 秒 |

沙箱未装 cron（`apt-get install cron` 无安装源），且**沙箱闲置会休眠**，
所以主通道走平台级定时任务。若要部署到自己的服务器：

```cron
30 8 * * 1-5 /workspace/web/run_daily_signal.sh >> /workspace/logs/daily_signal.log 2>&1
```

### 两轨输出（关键设计）

| 轨道 | 内容 | 是否执行 |
|---|---|---|
| **执行指令** | 按调仓日历判定。非调仓日 → 「维持上期持仓，不换股」 | ✅ 真金白银 |
| **每日观察** | 每天重算的六因子 / 12-1 动量排名 | ❌ 仅供观察 |

**为什么不每天换股**：个股腿按 v5 定案是**季频调仓**（1/4/7/10 月首个交易日）。
天天换手要付佣金 0.025% + 滑点 0.1% + 印花税 0.05%，而 v5 的回测收益正是
**按季频换手**验证出来的。日频调仓是另一个没有回测支撑的策略。

### 调仓日历

- 个股腿：**季频**，1/4/7/10 月首个交易日
- ETF 腿：**月频**，每月首个交易日
- 交易日来自新浪交易日历（`ak.tool_trade_date_hist_sina`），覆盖到 2026-12-31

### 三条口径铁律

1. **打分与闸门一律用 `bar_date`**（最后一个完整交易日收盘），绝不能用 `sig_date`。
   盘中运行时最后一根 K 线是未完成的，必须剔除，否则三道闸门因盘中价失真而
   **静默失效**，高价股（江波龙 369 元、中际旭创 859 元）会混进 top3。
2. **剔除无权限板块**：科创板 688/689、北交所 4xx/8xx、B 股 9xx；个股股价 ≤ 120 元。
3. **去重靠买入循环里的 `used_codes`**，不能用 `[c for c in hot if c not in stock]`。

### 数据新鲜度自检

`data_freshness` 字段报告 `stale` 与 `bars_behind`，**bar_date 落后会在页面顶部弹红色告警**，
绝不让旧因子静默生效。

**上游日线数据有延迟**：实测收盘后 4 分钟，`ak.stock_zh_a_daily` 最后一根仍是上一交易日
（通常要 15:30 之后才更新）。这不是代码问题，脚本会持续告警直到数据到位。

缓存新鲜度**按 bar_date 判定，不按文件修改时间**，且 manifest 记录的是**实际数据**
的 bar_date 而非期望值。这里踩过两个坑：

1. **按 mtime 判定**：早上 8:30 缓存 bar=09-02，当晚 20:00 再跑，文件才 11.5 小时
   「新鲜」，但 09-03 已收盘 → 拿旧因子给 09-04 下单。
2. **manifest 写期望值**：上游延迟时 manifest 记成 want_bar(09-03)，
   明早重跑 want_bar 正好也是 09-03 → **缓存永久命中，永远拿不到 09-02 之后的新数据**，
   重跑多少次都一样。

另外给上游延迟加了 30 分钟宽限（`attempted_for` 字段）：这段时间内不重复拉，
避免每次重跑白等 90 秒，但 stale 告警照常触发。`written_at` 只在**真的拉取过**时更新，
否则宽限期会被无限续期。

---

## v5 定案配置

| 项 | 值 |
|---|---|
| 总本金 | 200,000 元 |
| ETF 腿 | 12 只，月频调仓，合计 58% |
| 个股腿 | 季频调仓（1/4/7/10 月首个交易日） |
| ├ 防御仓 | 4 只 × 6.5% = 26%，修正六因子 |
| └ 热点仓 | 1 只 × 4% = 4%，12-1 动量 + 不追高三闸门 |
| 个股止损 | −25%（仅防黑天鹅） |
| 个股股价 | ≤ 120 元 |

### 关键公式

**防御仓 修正六因子**
```
(z(价值BP) + z(大市值) + z(12-1动量) + z(1月反转) − z(60日波动) + z(流动性)) / 6
```
小市值方向已反转。沪深300 池内 SMB 的 IC = −0.028~−0.051、t = −3.2~−6.9，
是负 alpha，写成 −z_size 等于常年倒着下注。

**热点仓 12-1 动量** = 近12个月收益 − 最近1个月收益（刻意剔掉最近一个月，
避开 A 股短期反转陷阱）

**三道不追高闸门**：近20日涨幅 ≤ +15% / 现价距52周高 ≤ 95% / CMF(20) ≥ −0.15

### 可执行口径

整手约束 100 股/份 + 佣金 0.025%（最低 5 元）+ 滑点 0.1%（买上浮卖下浮）
+ 卖出印花税 0.05%（ETF 免）。

**ETF 腿两轮分配**：第一轮按目标权重买，第二轮把剩余现金补给「目标金额买不满
1 手」的标的。十年国债 ETF 135.8 元、国债 ETF 118.2 元，1 手要 1.36 万 / 1.18 万，
而单只预算只有 9,667 元 —— 不补位就白白少 9.66% 仓位。修之前投入只有 74.2%，
修完 86.9%（与 v5 原始信号的 88% 吻合）。

---

## 已知局限（诚实面对）

- B vs A（纯防御）夏普差 +0.106，95%CI [−0.029, +0.256]，
  P(更优) = 86.9%，**未达 95% 统计显著**，不得表述为「显著提升」
- 回测缺质量因子（ROE/负债率/经营现金流），这是下一步最该补的
- 行业高度集中：防御仓 top12 里 7 只银行，本质「银行+」组合
- 回测区间偏牛，2022 年 +0.87% 是唯一熊市样本
- **重建的 `daily_signal.py` 与原始 `exec_engine.py` 无法逐行对齐**
  （原始脚本不在这个沙箱里）。PB 来自腾讯快照 p[46]，成分股池来自
  `index_stock_cons`，与原始口径可能有细微差别 —— 所以这次防御仓 Top4
  是紫金/工商/华夏/中国建筑，而上期记录是工商/农业/民生/兴业。
  分数差距很小（1.030 vs 0.741），但排名对口径敏感，这点必须说清楚

---

## 待办

1. 补质量因子（ROE/负债率/经营现金流），做成八因子重跑验证
2. 拆分 `daily_advisor.py`（92KB 1500+ 行）
   —— **注意：该文件不在这个沙箱里，需先找回**
3. git init 做版本管理（`.gitignore` 排除 `data/raw/`、`data/kline_cache/`）
4. 给 `download_*.py` 加增量更新逻辑
5. 补录 `web/api/positions.json` 的持仓成本价，让止损扫描真正跑起来
6. ETF 腿目前是**固定 12 只池**，没有实现真正的月度重排打分
   （v5 定案要求月频调仓，现在是「沿用上期名单」）

---

## 全量检查记录（2026-09-03 15:00 收盘后）

| 项 | 结果 |
|---|---|
| 8 个 API 端点 | 全部 HTTP 200，无 error 字段 |
| 5 个模块（在线 + `file://` 离线） | 全部有内容，0 真实 JS 错误，无 `undefined`/`NaN` |
| 模拟盘链路 | 买入/卖出/跟单 17/17/重置全部正常 |
| 非法单拦截 | 超持仓卖出、非整手、数量为 0、科创板、高价股、非法代码 —— 6/6 拦截 |
| 风险告警 | 止损线、单只超仓、行业集中度、现金垫 —— 构造场景后 4/4 触发 |
| 信号数据自检 | 整手、去重、权重上限、仓位纪律、bar<sig、三闸门 —— 34 项全通过 |
| 日期逻辑 | 周末/国庆正确跳过；周五盘后 sig_date 跳到下周一（不是周六） |
| 定时任务 | 周一至周五 08:30 ACTIVE，下次 2026-09-04 08:30 |

### 本轮查出并修复的问题

| # | 问题 | 后果 |
|---|---|---|
| 1 | 缓存 manifest 写 `want_bar` 而非实际 bar_date | 上游延迟时旧数据被永久标记为新鲜，重跑无效 |
| 2 | 每次命中缓存都刷新 `written_at` | 30 分钟宽限期被无限续期，永远等不到新数据 |
| 3 | B 股判定用 `startswith("9")` | `999999` 被误报成「B 股（无权限）」，排查时极误导 |
| 4 | 前端 `超额 ${PS((me-base).toFixed?0:0)}${PS(me-base)}pp` | 恒输出「0.000.00pp」 |
| 5 | 跟单说明硬编码「2026-09-02 收盘信号…26%/4%/58%/12%」 | 与当日信号对不上 |
| 6 | `add_paper_module.py` 仍存旧硬编码文案 | 重跑注入器会覆盖已修好的前端 |
| 7 | 底部旧快照指引 `src/daily_advisor.py --commit` | 该文件不在环境里，用户照做会碰壁 |

### 两个我核实后确认**不是** bug 的疑似问题

- `statement()` 里 `cash_weight`、`sectors[].weight` 存的**已经是百分比**（59.66），
  不是比率。命名有歧义但前端用法一致（不乘 100）
- 前端 `d.risks` 是**模块 3 个股分析**的字段（后端 `analyze_stock` 确实返回 `risks`），
  与模拟盘的 `alerts` 不是一回事
